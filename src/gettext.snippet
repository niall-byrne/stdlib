#!/bin/bash

STDLIB_TEXTDOMAINDIR="${STDLIB_TEXTDOMAINDIR-$(dirname -- "${BASH_SOURCE[0]}/locales")}"

export TEXTDOMAIN
export TEXTDOMAINDIR

set +e
builtin source gettext.sh1 2> /dev/null || stdlib.gettext.fallback
set -e
